export const ROUTES: unique symbol = Symbol("routes");
export const REDIRECTIONS: unique symbol = Symbol("redirections");
export const DESTROYED: unique symbol = Symbol("destroyed");

export default {
    ROUTES,
    REDIRECTIONS,
    DESTROYED
};